use admin
db.auth("root", "example")
use db_mongodb_00000000_insap;

db.createUser({
user: "user",
pwd: "example",
roles:[
    { "role": "clusterMonitor", "db": "admin" },
    "readWrite",
    "dbAdmin"
    ]
})
