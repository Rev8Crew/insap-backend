{
  "name": "insap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve:app": "cd ../insap-frontend && npm run serve",
    "build:app": "cd ../insap-frontend && npm run build",
    "lint:app": "cd ../insap-frontend && npm run lint"
  }
}
